@model Nokatak.Models.Vote

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Vote</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.NoktaID, "nokta")
        </div>
        <div class="editor-field">
            @Html.DropDownList("NoktaID", String.Empty)
            @Html.ValidationMessageFor(model => model.NoktaID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.up)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.up)
            @Html.ValidationMessageFor(model => model.up)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.userID)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.userID)
            @Html.ValidationMessageFor(model => model.userID)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
